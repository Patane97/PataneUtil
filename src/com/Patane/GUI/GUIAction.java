package com.Patane.GUI;

public interface GUIAction {
	/**
	 * Executes a command based on custom blocks of code.
	 * Create a new instance of GUIAction and @Override execute() to implement.
	 * @return
	 */
	public boolean execute();
	
}